{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Test1 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Civilité</th>
                <th>Name</th>
                <th>Firstname</th>
                <th>Ville</th>
                <th>Age</th>
                <th>Birthdate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('test1_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.civilite}}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.firstname }}</td>
                <td>{{ entity.ville }}</td>
                <td>{{ entity.age}}</td>
                <td>{% if entity.birthdate %}{{ entity.birthdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('test1_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('test1_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('test1_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
